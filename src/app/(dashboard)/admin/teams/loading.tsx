import { PageLoadingShell } from "@/components/ui/PageLoadingShell";

export default function Loading() {
  return (
    <PageLoadingShell
      title="Teams"
      subtitle="Manage teams and team-level permissions"
      variant="admin"
    />
  );
}
